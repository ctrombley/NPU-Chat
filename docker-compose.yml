services:
  app:
    build: .
    ports:
      - "1314:1314"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Uncomment to use PostgreSQL instead of SQLite:
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: npuchat
  #     POSTGRES_PASSWORD: npuchat
  #     POSTGRES_DB: npuchat
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
